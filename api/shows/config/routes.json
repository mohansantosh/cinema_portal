{
  "routes": [
    {
      "method": "GET",
      "path": "/shows",
      "handler": "Shows.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/count",
      "handler": "Shows.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/shows/:_id",
      "handler": "Shows.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/shows",
      "handler": "Shows.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/shows/:_id",
      "handler": "Shows.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/shows/:_id",
      "handler": "Shows.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}